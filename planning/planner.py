"""Planning module stubs."""

class Planner:
    """Generates high level plans for a given task."""

    def plan(self, task: str) -> list[str]:
        """Return a list of mock steps to accomplish the task."""
        return [f"Step for {task}"]
